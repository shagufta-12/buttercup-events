
<div class="container" style="height: 100vh">
  <div style="margin-top:20px"></div>

  <div class="alert alert-info alert-dismissible" role="alert" style="width:90%;">
    <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
    <strong>Sign in</strong> using any of this login credentials!  (<strong>Note: Each role has it's own abilities</strong>) <br>  <br>
    <div class="row">
      <div class="col-md-3">
        <h4 class="text-center">Admin</h4>
        - Name => Aman Sharma <br>
        - Email : admin@ButtercupEvents.com
      </div> 
      <div class="col-md-3">
        <h4 class="text-center">Organizer</h4>
        - Name => Rahul Singh<br>
        - Email : organizer@ButtercupEvents.com <br>
      </div> 
      <div class="col-md-3">
        <h4 class="text-center">Venue Owner</h4>
        - Name => Sachin Singh<br>
        - venue_owner@ButtercupEvents.com<br>
      </div> 
       <div class="col-md-3">
        <h4 class="text-center">Normal User</h4>
        - Name => Any One User<br>
        - Email : demo@example.com<br>
        - Password : 123***** <br>
      </div> 
     
    </div>
               
  </div>

  <div class="row">
    <div class="col-md-8">
      <div class="jumbotron jumbotron-login text-center">
            <h1>ButtercupEvents</h1>
            <p style="background: #000;">Every love story is beautiful, but yours should be unique</p>
            <p><a class="btn btn-warning btn-lg" href="/users/sign_up" role="button">Register for free</a></p>
      </div>
    </div>

    <div class="col-md-3">
      <h2>Log in</h2>

      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class="form-group">
          <%= f.label :email %><br />
          <%= f.email_field :email, autofocus: true, class: "form-control" %>
        </div>

        <div class="form-group">
          <%= f.label :password %><br />
          <%= f.password_field :password, autocomplete: "off", class: "form-control" %>
        </div>

        <% if devise_mapping.rememberable? -%>
          <div class="form-group">
            <%= f.check_box :remember_me %>
            <%= f.label :remember_me %>
          </div>
        <% end -%>

        <div class="actions">
          <%= f.submit "Log in", class: "btn btn-primary btn-lg btn-block" %>
        </div>
      <% end %>

      <%= render "devise/shared/links" %>
    </div>
  </div>
  
  
</div>